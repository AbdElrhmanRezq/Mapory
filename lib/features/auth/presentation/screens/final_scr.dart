import 'package:flutter/material.dart';

class FinalScr extends StatelessWidget {
  const FinalScr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Final Screen')));
  }
}
